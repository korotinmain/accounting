# 💰 Застосунок для Обліку Фінансів

Веб-застосунок на React для обліку фінансів з інтеграцією Firebase/Firestore. Дозволяє відстежувати початковий баланс, додавати записи за днями (ПІБ + сума), враховувати витрати на персонал і автоматично розраховувати залишок.

## 🚀 Функціонал

- **Початковий баланс**: встановлення та редагування початкового балансу
- **Записи по днях**: створення записів, згрупованих за датами
- **ПІБ та суми**: додавання кількох записів із ПІБ та сумами для кожного дня
- **Витрати на персонал**: облік персональних витрат для кожного дня
- **Автоматичний розрахунок залишку**: Залишок = Початковий баланс + Σ(суми) - Σ(персонал)
- **Гарний інтерфейс**: сучасний дизайн з градієнтами та анімаціями
- **Синхронізація з хмарою**: всі дані зберігаються в Firebase Firestore

## 📋 Вимоги

- Node.js (версія 14 або вище)
- npm або yarn
- Обліковий запис Firebase

## 🛠️ Встановлення

1. **Клонуйте репозиторій або перейдіть до директорії проекту:**

```bash
cd /Users/denyskorotin/Projects/accounting
```

2. **Встановіть залежності:**

```bash
npm install
```

3. **Налаштуйте Firebase:**

   a. Перейдіть на [Firebase Console](https://console.firebase.google.com/)

   b. Створіть новий проект або виберіть існуючий

   c. Увімкніть Firestore Database:
   - У меню зліва виберіть "Firestore Database"
   - Натисніть "Create database"
   - Виберіть режим "Start in test mode" (для розробки)
   - Виберіть регіон

   d. Отримайте конфігурацію Firebase:
   - Перейдіть в "Project Settings" (⚙️)
   - Прокрутіть вниз до "Your apps"
   - Виберіть веб-застосунок (</>)
   - Скопіюйте конфігурацію

   e. Відредагуйте файл `src/firebaseConfig.js` і вставте ваші дані:

```javascript
const firebaseConfig = {
  apiKey: "ваш-api-key",
  authDomain: "ваш-project-id.firebaseapp.com",
  projectId: "ваш-project-id",
  storageBucket: "ваш-project-id.appspot.com",
  messagingSenderId: "ваш-sender-id",
  appId: "ваш-app-id",
};
```

4. **Встановіть Firebase CLI (для деплою):**

```bash
npm install -g firebase-tools
```

5. **Увійдіть в Firebase:**

```bash
firebase login
```

6. **Ініціалізуйте Firebase у проекті:**

```bash
firebase init
```

- Виберіть "Hosting" та "Firestore"
- Виберіть існуючий проект
- Для "What do you want to use as your public directory?" введіть: `build`
- Для "Configure as a single-page app?" виберіть: `Yes`
- Для "Set up automatic builds and deploys with GitHub?" виберіть: `No`

## 🚀 Запуск проекту

**Запуск локального сервера розробки:**

```bash
npm start
```

Застосунок буде доступний за адресою [http://localhost:3000](http://localhost:3000)

## 📦 Деплой на Firebase

1. **Створіть production build:**

```bash
npm run build
```

2. **Задеплойте на Firebase Hosting:**

```bash
firebase deploy
```

Після успішного деплою ви отримаєте URL вашого застосунку.

## 📖 Як користуватися

1. **Встановіть початковий баланс:**
   - Натисніть кнопку "Редагувати" у секції "Початковий баланс"
   - Введіть суму
   - Натисніть "Зберегти"

2. **Додайте новий день:**
   - Виберіть дату
   - Введіть витрати на персонал (якщо є)
   - Додайте один або кілька записів з ПІБ та сумою
   - Натисніть "Зберегти день"

3. **Перегляд залишку:**
   - Поточний залишок автоматично розраховується та відображається у верхній частині

4. **Видалення дня:**
   - Натисніть кнопку "Видалити" на картці дня

## 🔧 Структура проекту

```
accounting/
├── public/
│   └── index.html           # HTML шаблон
├── src/
│   ├── App.js              # Головний компонент
│   ├── App.css             # Стилі
│   ├── index.js            # Точка входу
│   ├── firebase.js         # Ініціалізація Firebase
│   └── firebaseConfig.js   # Конфігурація Firebase
├── firebase.json           # Конфігурація Firebase
├── firestore.rules         # Правила безпеки Firestore
├── firestore.indexes.json  # Індекси Firestore
└── package.json            # Залежності проекту
```

## 🔐 Безпека

⚠️ **ВАЖЛИВО**: Поточні правила Firestore дозволяють доступ всім користувачам. Для production рекомендується:

1. Додати Firebase Authentication
2. Оновити правила безпеки в `firestore.rules`:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
```

## 🎨 Технології

- **React** 18.2.0 - UI фреймворк
- **Firebase** 10.7.1 - Backend та хостинг
- **Firestore** - NoSQL база даних
- **CSS3** - стилізація з градієнтами та анімаціями

## 📝 Ліцензія

MIT

## 👨‍💻 Автор

Створено з ❤️ для обліку фінансів

---

**Примітка**: Не забудьте замінити конфігурацію Firebase у файлі `src/firebaseConfig.js` на вашу власну!
