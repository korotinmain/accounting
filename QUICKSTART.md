# ⚡ Швидкий старт

## Перший запуск

```bash
# 1. Встановити залежності
npm install

# 2. Запустити (використовує існуючий .env)
npm start
```

Застосунок відкриється на http://localhost:3000

## Деплой на Firebase

```bash
# Повний деплой (hosting + rules)
npm run deploy

# Або окремо:
npm run deploy:hosting  # Тільки hosting
npm run deploy:rules    # Тільки firestore rules
```

## Важливі файли

- **App-optimized.js** - production версія (використовується замість App.js)
- **.env** - Firebase credentials (вже налаштовано)
- **constants.js** - всі константи та повідомлення
- **ErrorBoundary.js** - обробка помилок React

## Що нового (Production improvements)

✅ Environment variables для Firebase  
✅ Валідація всіх inputs  
✅ Error Boundary для React errors  
✅ SweetAlert2 замість стандартних alert  
✅ useCallback/useMemo оптимізації  
✅ ARIA labels для accessibility  
✅ Константи винесено  
✅ Детальна документація

## Якщо щось не працює

1. Перевірте консоль браузера (F12)
2. Перевірте `.env` файл
3. Переконайтесь що Firestore активовано в Firebase Console
4. Перевірте firestore rules: `npm run deploy:rules`

## Документація

- **README.md** - загальна інформація
- **DEPLOYMENT.md** - checklist для деплою
- **PRODUCTION-REVIEW.md** - список всіх покращень

Готово! 🚀
