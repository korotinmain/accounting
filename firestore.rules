rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Дозволити читання і запис для всіх користувачів
    // В продакшені краще додати аутентифікацію
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
